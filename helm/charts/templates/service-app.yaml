apiVersion: v1
kind: Service
metadata:
  name: go-portfolio-svc
  namespace: { { .Release.Namespace } }
spec:
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
  selector:
    role: go-portfolio
